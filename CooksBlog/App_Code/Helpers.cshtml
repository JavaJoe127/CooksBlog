@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<CooksBlog.Core.Objects.Tag> tags)
{
    foreach (var tag in tags)
    {
        <div class="tags-div">
            @CooksBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
    }
}